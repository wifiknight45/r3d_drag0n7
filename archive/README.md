# Archive

Pre-fix single-file variants kept for history (not deleted):

| File | Notes |
|------|--------|
| `sistema_enhanced.py` | Merge source of truth for the architecture fix (interpretations + pipeline). |
| `sistema_pre_fix.py` | Root `sistema.py` before the package split. |
| `sistema7.py` | Older interpretation-heavy variant. |
| `sistema_completa.py` | Intermediate combined variant. |

Canonical entrypoints now: `python sistema.py` or `python -m r3d_dragon`.
